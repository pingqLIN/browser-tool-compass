# Project instructions

This repository contains one static browser routing skill. `SKILL.md` is the
authoritative entrypoint; English documents are authoritative and `.zh-tw.md`
files are their Traditional Chinese companions.

- Preserve C1–C11 meanings and distinguish evidence gates, authority and action outcomes. Update corresponding JSON and both languages together.
- Keep the package portable: no machine paths, accounts, private endpoints, credentials, operational logs or dated workstation inventory in tracked files.
- Keep architecture planning, source provenance and local validation receipts in ignored `.local/`; do not stage them or `.del/`.
- Distribution is local-only and outside UniText registry coverage. No runtime projection or installer is supplied; the source folder is not an installed skill.
- This repository supplies semantic contracts, not executable adapters or live acceptance evidence. Do not infer browser availability from offline checks.
- Validate skill frontmatter, internal references, JSON and changed content before commit. Preserve reviewed bytes and use an explicit staging scope.
- No license grant is supplied. Publication, remote push, installation and runtime changes require their own applicable authority.
